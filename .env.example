# Base URL of the Kladdo backend REST API (include the /api suffix).
# Local dev falls back to http://localhost:8080/api when this is unset.
# On Render, set this to your backend service URL, e.g. https://kladdo-api.onrender.com/api
VITE_API_BASE_URL=http://localhost:8080/api
